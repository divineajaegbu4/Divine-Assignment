# Instruction

* Clone the Repository then change Directory  
* run the npm Install command

```bash
npm install
```

* Run the command below in order for the code to update automatically.

```bash
node --watch src/index.js
```

* Test the existing endpoint on postman

* Then add to: 

 > * Update by id 
 > * Delete by id 